# /etc/systemd/oomd.conf — base config (managed by optimize-memory.sh)
[OOM]
# How often oomd evaluates pressure (seconds)
DefaultMemoryPressureLimit=60%
DefaultMemoryPressureDurationSec=20s

# Keep oomd responsive
SwapUsedLimit=90%
