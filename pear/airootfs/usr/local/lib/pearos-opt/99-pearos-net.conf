# BBR congestion control + fair-queue: lower latency under load than Windows defaults
# BBR + fair-queue: low latency under load, better than Windows default CUBIC+FQ
net.core.default_qdisc = fq
net.ipv4.tcp_congestion_control = bbr
net.ipv4.tcp_fastopen = 3
